"use client";
/**
 * STUB — the real ChartChatDrawer is an agent-chat side panel (not on the public
 * API). Renders nothing; keeps the event-name exports so CockpitPage imports and
 * event wiring stay intact (they just never fire).
 */
export const CHART_CHAT_STATE_EVENT = "cockpit:chart-chat-state";
export const CHART_CHAT_CMD_EVENT = "cockpit:chart-chat-cmd";
export default function ChartChatDrawer() { return null; }
