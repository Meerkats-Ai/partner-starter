/**
 * STUB — the real launchRecommendationChat hands a recommendation to the agent
 * chat to resolve end-to-end (not on the public API). No-op; keeps imports clean.
 */
export function launchRecommendationChat() { /* no-op */ }
export function buildRecommendationTask() { return null; }
